{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "No frontend IPC permissions; the Rust core owns the spike runtime.",
  "windows": ["main"],
  "permissions": []
}
